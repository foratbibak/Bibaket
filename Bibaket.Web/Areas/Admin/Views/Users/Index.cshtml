@using Bibaket.Domain.ViewModels.User
@model AdminFilterUserViewModel

@{
    ViewData["Title"] = "لیست کاربران";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">افزودن کاربر جدید</a>
</p>
@if (ViewBag.Create == "Success")
{
    <div class="alert alert-success">
        <h6>کاربر با موفقیت ثبت شد</h6>
    </div>
}
<table class="table">
    <thead>
        <tr>
            <th>
                آواتار
            </th>
            <th>
                نام
            </th>
            <th>
                نام خانوادگی
            </th>
            <th>
                نام کاربری 
            </th>
            <th>
                ایمیل 
            </th>
            <th>
                موبایل
            </th>
            <th>
                فعال/غیرفعال
            </th>
            <th>
                تاریخ ایجاد
            </th>
            <th>
                تاریخ ویرایش
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Entities)
        {
            <tr style="@(item.IsDelete ? "color:Red;" : "")">
                <td>
                    <img src="/Avatars/@item.Avatar" class="user-profile-img" style="max-width: 75px;border-radius: 15px;" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Mobile)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.IsActive)
                </td>
                <td>
                    @item.CreatDate.ToShamsi()
                </td>
                <td>
                    @item.UpdateDate?.ToShamsi()
                </td>
                <td>
                    @item.LastLoginDate?.ToShamsi()
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">ویرایش</a>
                    <a asp-action="Details" asp-route-id="@item.Id" style="color: #28a745;">جزئیات</a>
                    <a asp-action="Delete" asp-route-id="@item.Id">حذف</a>

                    @* @if (item.IsDelete == false)
                    {

                    } *@
                    @* else
                    {
                        <a asp-action="DeDelete" asp-route-id="@item.Id" style="color: green;">بازگردانی</a>
                    } *@
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />
<partial name="_AdminPagination" model="Model.GetCurrentPaging()" />















